<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

<%= link_to "edit",  edit_task_path(@task.id) %>

<%= link_to "delete",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
